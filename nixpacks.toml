[phases.setup]
nixPkgs = ["python313"]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["ffmpeg -version"]

[start]
cmd = "python bot_telegram.py"