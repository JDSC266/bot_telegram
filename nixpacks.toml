[phases.setup]
nixPkgs = ["ffmpeg", "ffmpeg-full", "python313"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["ffmpeg -version"]

[start]
cmd = "python bot_telegram.py"